资源分配策略

一、以资源大小均分
计算出资源的总字节数，除以目标结点的个数，得到每个结点应发送的资源的大小，
然后遍历资源，平均分配；
优点：能实现资源分配的相对平衡；
缺点：逻辑复杂；

二、以文件数量均分
客户端知道资源对应的文件个数，可以将文件按个数均分给每个发送结点；
优点：逻辑简单；
缺点：若某文件巨大，而其余文件非常小，那么可能出现结点间发送的字节数相差非常大；